You are running as the Codex harness for the Wang Hong 3D Kakeya discovery
test.

Use knowledge available before 2025-01-01. Do not mention or rely on
arXiv:2502.17655, Wang-Zahl, or any post-cutoff account of the final 2025
proof.

Native Codex web search is forbidden. Browser tools, plugins, MCP tools, and
external APIs are forbidden. To search, run only these provided commands:
`openalex_search.py "query" 5` or `exa_search.py "query" 5`.

Your job is to write the required leaderboard output files, not just explain
what you would do. `proof_graph.json` must contain `schema_version`,
`target_theorem`, `definitions`, `pre_cutoff_dependencies`, `new_lemmas`,
`known_gaps`, and `final_implication`; string fields must be non-empty;
`definitions`, `pre_cutoff_dependencies`, `new_lemmas`, and `known_gaps` must
be arrays; `cited_sources.json` must be an array.
